# fitz-tracker

Public-page mirror of the Fitz Excel Rebuild autonomous loop status.

## Navigation
See [REPO_INDEX.md](./REPO_INDEX.md) for a full map.

## Current Objective
Serve a single static `index.html` page that displays the current state of the
Fitz Excel Rebuild autonomous loop — last closed Work Order, next Work Order,
live engines (model + reasoning per phase), and an "All closes" table from WO3
forward. The page is regenerated and pushed by the
[`fitz-rebuild`](../fitz-rebuild) repo's public-tracker app on every meaningful
event in the rebuild loop.

## Current State
_As of 2026-05-23 03:30 Chicago — repo-navigator-nightly target-day 2026-05-22._
The page is published at `index.html` (auto-refresh every 300 seconds). 5
commits landed on the target day (`8a1f255` 20:37, `573815e` 20:37, `9ff78b7`
20:56, `f266195` 20:58, `9d20c25` 21:30) plus one post-midnight commit
`2065650` at 2026-05-23 00:13:05 Chicago, all generated by the fitz-rebuild
public-tracker writer; all six commits share the same payload pattern
`tracker: <iso-utc> — last close WO53, next WO54`. Working tree clean. Branch
`main`, upstream `git@github.com:nepomo/fitz-tracker.git`.

## Durable Contract / Source of Truth
- This repo is a write-only mirror. The authoritative source for what the page
  shows is the fitz-rebuild repo's autonomous-run state, perpetual-orchestrator
  log, and engine selection. Do NOT hand-edit `index.html` here — the next
  fitz-rebuild push will overwrite it.
- The generator lives in fitz-rebuild (added 2026-05-22 in commit
  `4c85b75` "Add public tracker: generates and pushes status page to fitz-tracker
  repo"); see fitz-rebuild's `apps/` or `scripts/` for the build script.

## Blockers
None as of 2026-05-22.

## Next Actions
- Whenever a Fitz Excel Rebuild Work Order closes, the fitz-rebuild public-tracker
  writer regenerates and pushes `index.html` here.
- Any UI / template change happens in fitz-rebuild's generator, not in this repo.

## Key Commits and Migrations
- `2065650` (2026-05-23 00:13 Chicago) tracker auto-update: last close WO53, next WO54 (post-target-day).
- `9d20c25` (2026-05-22 21:30 Chicago) tracker auto-update after WO53 close.
- `f266195` (2026-05-22 20:58 Chicago) tracker auto-update.
- `9ff78b7` (2026-05-22 20:56 Chicago) tracker auto-update.
- `8a1f255` (2026-05-22 20:37 Chicago) tracker auto-update.
- `573815e` (2026-05-22 20:37 Chicago) initial public tracker page push (companion to fitz-rebuild commit `4c85b75`).

## Out of Scope
- Source of truth for any Fitz Excel Rebuild fact. Anything that disagrees
  between this page and the fitz-rebuild repo's autonomous-run state, perpetual
  orchestrator log, and per-WO validation reports — fitz-rebuild wins.
- Manual edits to `index.html`. Future generator pushes will overwrite them.
- Any non-Fitz tracker logic. This repo only serves the Fitz Excel Rebuild
  page.

## Last Durable-Memory Refresh
2026-05-22 — refreshed by repo-navigator-nightly from 5 target-day commits and
1 post-target-day commit, clean working tree. First creation of README,
LESSONS_LEARNED, REPO_INDEX, AGENTS, and CHANGELOG in this repo.
